# 问题清单

## 后端问题

1. MyBatis的动态SQL + 分页插件，在CASE WHEN，动态SQL嵌套下会严重影响性能。（2025-05-04）
2. MyBatis的分页插件，只对这个调用链中的第一条SQL生效 为什么呢？解决方案是什么呢？（2025-05-04）



## 前端问题

1. 前端CSS的 `transform: scale(0.5)` 不会改变元素本身的width和height,只是渲染的时候，会缩小。（2025-05-04）
2. 在前端实现UI交互效果的时候，如果使用CSS实现非常困难的时候，这个时候可以使用JS来辅助实现，尤其是一些比较灵活的效果。**重温浏览器的渲染原理**（2025-05-04）
3. 前端Flex布局，细节其实有很多，比如在嵌套的Flex布局中，内层的height可能不会生效，可以尝试使用min-height来试试 为什么呢？（2025-05-04）
4. 在前端中使用JS获取某个元素的scrollWidth是一个整数，而且还是一个被四舍五入的整数，为什么呢？（2025-05-04）
5. 在前端中，Error对象中的字段是不能为JSON序列化字符串，就连message字段都不可以，为什么呢？（2025-05-04）



## 工作习惯问题

1. 坚持自己的工作习惯，先列任务文档，再去实际干活，需要规划出，今天最少需要完成到那里。



## 值得探索的领域

1. 音视频的处理流程。（2025-05-04）



